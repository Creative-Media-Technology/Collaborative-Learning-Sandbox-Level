using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class hopeitworks : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //lets pray
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
