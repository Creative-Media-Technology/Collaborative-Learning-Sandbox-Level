using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tesingScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //This is a script
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
